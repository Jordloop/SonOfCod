<!DOCTYPE html>

<html>
<head>

    <meta name="viewport" content="width=device-width" />
    <link href="~/css/style.css" rel="stylesheet" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div class="flex-nav">
        <div class="logo"></div>
        <p class="option nav-option">@Html.ActionLink("Home", "Index", "Marketing")</p>
        <p class="option nav-option">@Html.ActionLink("Newsletter", "Index", "Newsletter")</p>
        <p class="option nav-option">@Html.ActionLink("Admin Portal", "Login", "Admin")</p>
    </div>
    <div class="flex-container">
        @RenderBody()
    </div>
</body>
</html>
